dist: bionic

language: minimal

install:
  - curl -fsSL https://get.docksal.io | bash
  - fin version
  - fin sysinfo

before_script:
  - fin init

script:
  - fin drush si --db-url=mysql://root:root@db/default
  - fin test
